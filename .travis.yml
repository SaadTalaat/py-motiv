---
dist: xenial
language: python
python:
  - "3.7"

before_install:
  - pip install nose nose-cov

install:
  - pip install -r requirements.txt
  - python setup.py -q install

script:
  - nosetests
  - pylint motiv/
